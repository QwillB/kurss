<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Style x:Key="EnterpriseCard" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource EnterpriseCardBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource EnterpriseBorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CornerRadius" Value="8"/>
        <Setter Property="Padding" Value="14"/>
        <Setter Property="Margin" Value="6"/>
    </Style>

    <Style x:Key="SidebarButton" TargetType="Button">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{StaticResource EnterpriseMutedTextBrush}"/>
        <Setter Property="BorderBrush" Value="Transparent"/>
        <Setter Property="Padding" Value="14,10"/>
        <Setter Property="HorizontalContentAlignment" Value="Left"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}" CornerRadius="7" Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#22304A"/>
                            <Setter Property="Foreground" Value="{StaticResource EnterpriseTextBrush}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="EnterpriseButton" BasedOn="{StaticResource PrimaryButton}" TargetType="Button">
        <Setter Property="Background" Value="{StaticResource EnterpriseAccentBrush}"/>
        <Setter Property="Foreground" Value="#08111F"/>
        <Setter Property="Height" Value="34"/>
    </Style>

    <Style x:Key="EnterpriseOutlineButton" BasedOn="{StaticResource OutlineButton}" TargetType="Button">
        <Setter Property="Foreground" Value="{StaticResource EnterpriseAccentBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource EnterpriseBorderBrush}"/>
        <Setter Property="Height" Value="34"/>
    </Style>
</ResourceDictionary>

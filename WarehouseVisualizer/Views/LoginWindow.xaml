<Window x:Class="WarehouseVisualizer.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WarehouseVisualizer"
        xmlns:conv="clr-namespace:WarehouseVisualizer.Converters"
        mc:Ignorable="d"
        Title="Warehouse Visualizer - Авторизация" 
        Height="650" Width="500"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="SingleBorderWindow">

    <Window.Resources>
        <conv:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>

        <Style TargetType="TextBox">
            <Setter Property="Padding" Value="8"/>
            <Setter Property="Margin" Value="0,5,0,15"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>

        <Style TargetType="PasswordBox">
            <Setter Property="Padding" Value="8"/>
            <Setter Property="Margin" Value="0,5,0,15"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>

        <Style TargetType="Button">
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="Margin" Value="0,10,0,0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Background" Value="#2196F3"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>

        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#333"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>
    </Window.Resources>

    <Grid Background="#F5F5F5">
        <Border Background="White" 
                CornerRadius="10" 
                Margin="30"
                Padding="30"
                VerticalAlignment="Center">
            <StackPanel>
                <!-- Заголовок -->
                <StackPanel Margin="0,0,0,30">
                    <TextBlock Text="WAREHOUSE VISUALIZER" 
                               FontSize="24" 
                               FontWeight="Bold" 
                               Foreground="#2196F3"
                               HorizontalAlignment="Center"/>
                    <TextBlock Text="Система управления складом" 
                               FontSize="14" 
                               HorizontalAlignment="Center"
                               Margin="0,5,0,0"
                               Foreground="#666"/>
                </StackPanel>

                <!-- Форма входа -->
                <TextBlock Text="Вход в систему" 
                           FontSize="16" 
                           FontWeight="SemiBold"
                           Margin="0,0,0,20"/>

                <!-- Поле логина -->
                <TextBlock Text="Логин"/>
                <TextBox x:Name="UsernameTextBox" 
                         Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}"
                         MaxLength="50"/>

                <!-- Поле пароля -->
                <TextBlock Text="Пароль"/>
                <PasswordBox x:Name="PasswordBox" 
                             PasswordChanged="PasswordBox_PasswordChanged"
                             MaxLength="50"/>

                <!-- Кнопка входа -->
                <Button Content="Войти" 
                        Command="{Binding LoginCommand}"
                        IsDefault="True"
                        Height="45"/>

                <!-- Сообщение об ошибке -->
                <TextBlock Text="{Binding ErrorMessage}" 
                           Foreground="Red" 
                           Margin="0,15,0,0"
                           TextWrapping="Wrap"
                           HorizontalAlignment="Center"
                           Visibility="{Binding HasError, Converter={StaticResource BooleanToVisibilityConverter}}"/>

                <!-- Информация о демо-доступе -->
                <Border Background="#E3F2FD" 
                        CornerRadius="5" 
                        Padding="10" 
                        Margin="0,20,0,0">
                    <StackPanel>
                        <TextBlock Text="Демо-доступ:" 
                                   FontWeight="Bold" 
                                   Foreground="#1565C0"/>
                        <TextBlock Text="admin / 123456" 
                                   Margin="0,5,0,0"/>
                        <TextBlock Text="storekeeper1 / 123456" 
                                   Margin="0,2,0,0"/>
                        <TextBlock Text="auditor1 / 123456" 
                                   Margin="0,2,0,0"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </Border>
    </Grid>
</Window>